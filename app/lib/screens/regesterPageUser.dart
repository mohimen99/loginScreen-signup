import 'package:app/components/regesterDesignUser.dart';
import 'package:flutter/material.dart';

class regesterPageUser extends StatelessWidget {
  const regesterPageUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return regester_design();
  }
}
